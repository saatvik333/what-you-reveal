/**
 * Bait file for AdBlock Detection (Execution Vector)
 * 
 * This file sets a global flag. If this script runs, we know that
 * script blocking (for ad-related filenames) is NOT active.
 */
window.ad_script_loaded = true;
console.log('Ad script loaded (Execution Vector Verified).');
